/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type WithdrawExcessLamportsInstructionAccounts = {
  source: PublicKey | Pda;
  destination: PublicKey | Pda;
  authority?: Signer;
};

// Data.
export type WithdrawExcessLamportsInstructionData = { discriminator: number };

export type WithdrawExcessLamportsInstructionDataArgs = {};

export function getWithdrawExcessLamportsInstructionDataSerializer(): Serializer<
  WithdrawExcessLamportsInstructionDataArgs,
  WithdrawExcessLamportsInstructionData
> {
  return mapSerializer<
    WithdrawExcessLamportsInstructionDataArgs,
    any,
    WithdrawExcessLamportsInstructionData
  >(
    struct<WithdrawExcessLamportsInstructionData>([['discriminator', u8()]], {
      description: 'WithdrawExcessLamportsInstructionData',
    }),
    (value) => ({ ...value, discriminator: 38 })
  ) as Serializer<
    WithdrawExcessLamportsInstructionDataArgs,
    WithdrawExcessLamportsInstructionData
  >;
}

// Instruction.
export function withdrawExcessLamports(
  context: Pick<Context, 'identity' | 'programs'>,
  input: WithdrawExcessLamportsInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'splToken',
    'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'
  );

  // Accounts.
  const resolvedAccounts: ResolvedAccountsWithIndices = {
    source: { index: 0, isWritable: true, value: input.source ?? null },
    destination: {
      index: 1,
      isWritable: true,
      value: input.destination ?? null,
    },
    authority: { index: 2, isWritable: false, value: input.authority ?? null },
  };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getWithdrawExcessLamportsInstructionDataSerializer().serialize(
    {}
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
