/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountsOptions,
  assertAccountExists,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import { Mint, deserializeMint } from './mint';
import { Token, deserializeToken } from './token';
import { Multisig, deserializeMultisig } from './multisig';

export type SplTokenAccount = Mint | Token | Multisig;

export function deserializeSplTokenAccount(
  rawAccount: RpcAccount
): SplTokenAccount {
  const data = rawAccount.data;
  if (data.length === 82) {
    return deserializeMint(rawAccount);
  }
  if (data.length === 165) {
    return deserializeToken(rawAccount);
  }
  if (data.length === 355) {
    return deserializeMultisig(rawAccount);
  }
  throw new Error(
    'The provided account could not be identified as a splToken account.'
  );
}

export async function fetchAllSplTokenAccounts(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<SplTokenAccount[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount);
    return deserializeSplTokenAccount(maybeAccount);
  });
}

export async function safeFetchAllSplTokenAccounts(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<(SplTokenAccount | null)[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    if (!maybeAccount.exists) return null;
    return deserializeSplTokenAccount(maybeAccount);
  });
}
