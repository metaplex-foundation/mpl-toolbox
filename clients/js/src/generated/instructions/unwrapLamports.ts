/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  option,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type UnwrapLamportsInstructionAccounts = {
  source: PublicKey | Pda;
  destination: PublicKey | Pda;
  authority?: Signer;
};

// Data.
export type UnwrapLamportsInstructionData = {
  discriminator: number;
  amount: Option<bigint>;
};

export type UnwrapLamportsInstructionDataArgs = {
  amount: OptionOrNullable<number | bigint>;
};

export function getUnwrapLamportsInstructionDataSerializer(): Serializer<
  UnwrapLamportsInstructionDataArgs,
  UnwrapLamportsInstructionData
> {
  return mapSerializer<
    UnwrapLamportsInstructionDataArgs,
    any,
    UnwrapLamportsInstructionData
  >(
    struct<UnwrapLamportsInstructionData>(
      [
        ['discriminator', u8()],
        ['amount', option(u64())],
      ],
      { description: 'UnwrapLamportsInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 45 })
  ) as Serializer<
    UnwrapLamportsInstructionDataArgs,
    UnwrapLamportsInstructionData
  >;
}

// Args.
export type UnwrapLamportsInstructionArgs = UnwrapLamportsInstructionDataArgs;

// Instruction.
export function unwrapLamports(
  context: Pick<Context, 'identity' | 'programs'>,
  input: UnwrapLamportsInstructionAccounts & UnwrapLamportsInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'splToken',
    'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'
  );

  // Accounts.
  const resolvedAccounts: ResolvedAccountsWithIndices = {
    source: { index: 0, isWritable: true, value: input.source ?? null },
    destination: {
      index: 1,
      isWritable: true,
      value: input.destination ?? null,
    },
    authority: { index: 2, isWritable: false, value: input.authority ?? null },
  };

  // Arguments.
  const resolvedArgs: UnwrapLamportsInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getUnwrapLamportsInstructionDataSerializer().serialize(
    resolvedArgs as UnwrapLamportsInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
