/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountsOptions,
  assertAccountExists,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import { u32 } from '@metaplex-foundation/umi/serializers';
import {
  AddressLookupTable,
  deserializeAddressLookupTable,
} from './addressLookupTable';

export type SplAddressLookupTableAccount = AddressLookupTable;

function accountDataMatches(
  data: Uint8Array,
  expected: Uint8Array,
  offset: number
): boolean {
  if (data.length < offset + expected.length) return false;
  for (let i = 0; i < expected.length; i++) {
    if (data[offset + i] !== expected[i]) return false;
  }
  return true;
}

export function deserializeSplAddressLookupTableAccount(
  rawAccount: RpcAccount
): SplAddressLookupTableAccount {
  const data = rawAccount.data;
  if (accountDataMatches(data, u32().serialize(1), 0)) {
    return deserializeAddressLookupTable(rawAccount);
  }
  throw new Error(
    'The provided account could not be identified as a splAddressLookupTable account.'
  );
}

export async function fetchAllSplAddressLookupTableAccounts(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<SplAddressLookupTableAccount[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount);
    return deserializeSplAddressLookupTableAccount(maybeAccount);
  });
}

export async function safeFetchAllSplAddressLookupTableAccounts(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<(SplAddressLookupTableAccount | null)[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    if (!maybeAccount.exists) return null;
    return deserializeSplAddressLookupTableAccount(maybeAccount);
  });
}
